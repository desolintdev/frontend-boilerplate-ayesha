import { LuPanelLeft } from "react-icons/lu";

import { IconProps } from "@/shared/interfaces/common";

export default function PanelLeftIcon(props: IconProps) {
  return <LuPanelLeft {...props} />;
}
