import {FaClipboardList} from 'react-icons/fa6';

import {IconProps} from '@/shared/interfaces/common';

export default function TasksIcon(props: IconProps) {
  return <FaClipboardList {...props} />;
}
