import { IoMenuOutline } from "react-icons/io5";

import { IconProps } from "@/shared/interfaces/common";

export default function BarsIcon(props: IconProps) {
  return <IoMenuOutline {...props} />;
}
