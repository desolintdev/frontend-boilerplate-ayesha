export interface LocalLanguages {
  [key: string]: {
    value: string;
    label: string;
    flag: string;
  };
}

export interface LanguageOption {
  value: string;
  label: string;
  flag: string;
}
